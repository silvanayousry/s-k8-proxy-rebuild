apiVersion: autoscaling/v2beta1 
kind: HorizontalPodAutoscaler 
metadata: 
  name: {{ template "fullname" . }}-nginx
  labels:
    app: {{ template "appname" . }}-nginx
spec: 
  maxReplicas: {{ .Values.scale.MaxReplicaCount }}
  minReplicas: {{ .Values.scale.MinReplicaCount }}  
  scaleTargetRef: 
    apiVersion: extensions/v1beta1
    kind: Deployment 
    name: {{ template "trackableappname" . }}-nginx
  metrics: 
  - type: Resource 
    resource: 
      name: cpu 
      targetAverageUtilization: 75
  - type: Resource 
    resource: 
      name: memory 
      targetAverageUtilization: 75
---
apiVersion: autoscaling/v2beta1 
kind: HorizontalPodAutoscaler 
metadata: 
  name: {{ template "fullname" . }}-squid
  labels:
    app: {{ template "appname" . }}-squid
spec: 
  maxReplicas: {{ .Values.scale.MaxReplicaCount }}
  minReplicas: {{ .Values.scale.MinReplicaCount }}  
  scaleTargetRef: 
    apiVersion: extensions/v1beta1
    kind: Deployment 
    name: {{ template "trackableappname" . }}-squid
  metrics: 
  - type: Resource 
    resource: 
      name: cpu 
      targetAverageUtilization: 75
  - type: Resource 
    resource: 
      name: memory 
      targetAverageUtilization: 80
